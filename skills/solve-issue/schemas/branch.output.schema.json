{
  "type": "object",
  "required": ["branch", "created", "verified"],
  "properties": {
    "branch": { "type": "string" },
    "created": { "type": "boolean" },
    "verified": {
      "type": "string",
      "description": "The raw output from git branch --show-current — must equal the branch name"
    }
  }
}
